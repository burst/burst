#include "recording_thread.h"

void RecordingThread::run()
{
    // open file
    // write to it from mutex
    while (m_continue) {
        //get mutex
        // get current index, current values
        //leave mutex
        // compress current index, current values
    }
    // close file
}


